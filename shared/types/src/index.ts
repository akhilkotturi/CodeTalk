// Shared TypeScript types/interfaces used across services.
// Intentionally empty for now — add types here as incident-service and
// later services need to share shapes (e.g., an Incident DTO once the
// REST contract is settled), rather than pre-defining them speculatively.

export {};
