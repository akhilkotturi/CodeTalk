PORT=4001

# Same Postgres instance as incident-service — snippets table lives in the same DB
DATABASE_URL=postgres://war_room:war_room@localhost:5432/incident_service

# JWT signing secret — must match incident-service
JWT_SECRET=change-me-in-production
